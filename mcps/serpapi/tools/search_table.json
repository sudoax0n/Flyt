{
  "name": "search_table",
  "description": "Interactive UI variant of `search`: returns organic results as a sortable, searchable table rendered in the conversation. Same params as `search`. Use when the host supports MCP Apps and the user wants to browse results visually rather than read JSON.",
  "inputSchema": {
    "additionalProperties": false,
    "properties": {
      "params": {
        "additionalProperties": true,
        "default": null,
        "type": "object"
      }
    },
    "type": "object"
  }
}